let redValue = 255;
let greenValue = 0;
let blueValue = 0;

function setup() {
  createCanvas(400, 400);
}

function draw() {
  background(220);
  fill(redValue, greenValue, blueValue);
  ellipse(200, 200, 100, 100);
}

